<div class="w-64 bg-white shadow-md flex flex-col h-screen">
    <!-- Logo -->
    <div class="flex items-center justify-center h-16 border-b">
        <a href="{{ route('dashboard') }}" class="flex items-center space-x-2">
            <x-application-logo class="block h-8 w-auto fill-current text-gray-800" />
            <span class="font-bold text-lg text-gray-700">Painel</span>
        </a>
    </div>

    <!-- Links -->
    <nav class="flex-1 px-4 py-6 space-y-2">
        <x-nav-link :href="route('dashboard')" :active="request()->routeIs('dashboard')" class="block">
            🏠 {{ __('Início') }}
        </x-nav-link>

        <x-nav-link :href="route('produtos.index')" :active="request()->routeIs('produtos.*')" class="block">
            🧴 {{ __('Produtos') }}
        </x-nav-link>

        <x-nav-link :href="route('categorias.index')" :active="request()->routeIs('categorias.*')" class="block">
            🧩 {{ __('Categorias') }}
        </x-nav-link>

        <x-nav-link :href="route('revendedoras.index')" :active="request()->routeIs('revendedoras.*')" class="block">
            👩‍💼 {{ __('Revendedoras') }}
        </x-nav-link>

        <x-nav-link :href="route('equiperevenda.index')" :active="request()->routeIs('equiperevenda.*')" class="block">
            👥 {{ __('Equipes de Revenda') }}
        </x-nav-link>

        <x-nav-link :href="route('supervisores.index')" :active="request()->routeIs('supervisores.*')" class="block">
            🧑‍🏫 {{ __('Supervisores') }}
        </x-nav-link>

        <x-nav-link :href="route('clientes.index')" :active="request()->routeIs('clientes.*')" class="block">
            🧾 {{ __('Clientes') }}
        </x-nav-link>
    </nav>

    <!-- Usuário logado -->
    <div class="border-t px-4 py-3 bg-gray-50">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-gray-800">{{ Auth::user()->nome }}</p>
                <p class="text-xs text-gray-500">{{ Auth::user()->email }}</p>
            </div>
            <form method="POST" action="{{ route('logout') }}">
                @csrf
                <button type="submit" class="text-sm text-red-500 hover:text-red-700">
                    {{ __('Sair') }}
                </button>
            </form>
        </div>
    </div>
</div>
